"""1)User Input and Replace String Template “Hello <<UserName>>, How are you?”
a. I/P -> Take User Name as Input. Ensure UserName has min 3 char
b. Logic -> Replace <<UserName>> with the proper name
c. O/P -> Print the String with User Name"""

name = input("Enter the name: ")
if len(name) >= 3:
    print(f"Hello {name}, How are you?")
else:
    print("Invalid Name")


"""2) Flip Coin and print percentage of Heads and Tails
a. I/P -> The number of times to Flip Coin. Ensure it is positive integer.
b. Logic -> Use Random Function to get value between 0 and 1. If < 0.5 then tails or
heads
c. O/P -> Percentage of Head vs Tails"""

#Using Random
import random
n = int(input("Enter number of flips: "))
if n <= 0:
    print("Number must be positive")
else:
    heads = 0
    tails = 0

    for _ in range(n):
        if random.random() < 0.5:
            tails += 1
        else:
            heads += 1
    print("Heads Percentage:", (heads / n) * 100)
    print("Tails Percentage:", (tails / n) * 100)


"""3) Leap Year
a. I/P -> Year, ensure it is a 4 digit number.
b. Logic -> Determine if it is a Leap Year.
c. O/P -> Print the year is a Leap Year or not."""

year = int(input("Enter year: "))
if 1000 <= year <= 9999:
    if (year % 400 == 0) or (year % 4 == 0 and year % 100 != 0):
        print(year, "is a Leap Year")
    else:
        print(year, "is not a Leap Year")
else:
    print("Enter a valid 4-digit year")


"""4. Power of 2 
a. Desc -> This program takes a command-line argument N and prints a table of the powers of 2 that are less than or equal to 2^N. 
b. I/P -> The Power Value N. Only works if 0 <= N < 31 since 2^31 overflows an int 
c. Logic -> repeat until i equals N. d. O/P -> Print the year is a Leap Year or not."""

N = int(input("Enter N: "))
if 0 <= N < 31:
    for i in range(N + 1):
        print(f"2^{i} = {2 ** i}")
else:
    print("N must be between 0 and 30")

"""5. Harmonic Number a. Desc -> Prints the Nth harmonic number: 1/1 + 1/2 + ... + 1/N (http://users.encs.concordia.ca/~chvatal/notes/harmonic.html). 
b. I/P -> The Harmonic Value N. Ensure N != 0 
c. Logic -> compute 1/1 + 1/2 + 1/3 + ... + 1/N 
d. O/P -> Print the Nth Harmonic Value."""

N = int(input("Enter N: "))
if N == 0:
    print("N must not be zero")
else:
    harmonic = 0.0
    for i in range(1, N + 1):
        harmonic += 1 / i

    print("Harmonic Value:", harmonic)

"""6. Factors a. Desc -> Computes the prime factorization of N using brute force. 
b. I/P -> Number to find the prime factors 
c. Logic -> Traverse till i*i <= N instead of i <= N for efficiency. 
d. O/P -> Print the prime factors of number N."""

N = int(input("Enter number: "))
for i in range(2, N + 1):
    while N % i == 0:
        print(i, end=" ")
        N //= i
print("No prime Numbers")

